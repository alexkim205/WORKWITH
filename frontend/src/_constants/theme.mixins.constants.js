import styledBreakpoint from '@humblebee/styled-components-breakpoint';
import { breakpoints } from './theme.constants';

export const breakpoint = styledBreakpoint(breakpoints);

export const mixinfiller = 1;
