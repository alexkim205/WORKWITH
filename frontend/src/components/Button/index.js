import {
  StyledFormButton,
  StyledModalButton,
  StyledLinkButton,
  StyledIconButton
} from './Button.style';

export const FormButton = StyledFormButton;
export const ModalButton = StyledModalButton;
export const LinkButton = StyledLinkButton;
export const IconButton = StyledIconButton;
